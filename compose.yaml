services:
  slam:
    image: basketbuddy/slam:humble
    restart: unless-stopped
    container_name: slam
    volumes:
      - ./config/slam_params.yaml:/slam_params.yaml
      - ./maps:/maps
    command: >
      ros2 launch slam_toolbox online_sync_launch.py 
        slam_params_file:=/slam_params.yaml
